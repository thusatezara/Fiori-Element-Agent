# {{projectName}}

Standard Fiori Elements application generated by the reusable `001 → 002` protocol.

## Service

- OData version: {{odataVersion}}
- Service root: {{serviceRoot}}
- EntitySet: {{entitySet}}
- EntityType: {{entityType}}
- Key: {{keys}}
- Properties: {{properties}}

## Run

```bash
npm install
npm start
```

`npm start` opens the local FLP Sandbox at `test/flpSandbox.html` with the `{{flpInboundKey}}` intent. Use `npm run start-noflp` for the standalone `index.html` preview. The generated `ui5.yaml` reads the configured service through the Fiori Tools proxy. No backend or deployment change is performed.

## Validate

```bash
npm run lint
npm run build
```
