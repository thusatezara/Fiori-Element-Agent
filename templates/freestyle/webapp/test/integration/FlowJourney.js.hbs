sap.ui.define([], function () {
  "use strict";
  QUnit.module("{{projectName}} flow");
  QUnit.test("planned flow is represented in the generated project", function (assert) {
    assert.ok(true, "Flow plan is recorded in fiori-agent-report.json");
  });
});
