# {{projectName}}

Freestyle SAPUI5 application generated by the reusable `001 → 004` protocol.

## Service

- OData version: {{odataVersion}}
- Service root: {{serviceRoot}}
- EntitySet: {{entitySet}}
- EntityType: {{entityType}}
- Key: {{keys}}
- Properties: {{properties}}

## Design

The OData model owns business data. The named `view` JSONModel owns only client screen state. UI layout and interaction are expressed in XML Views and controllers; UI annotations are not used to decide the screen.

## Run

```bash
npm install
npm start
```

## Validate

```bash
npm run lint
npm run build
```
