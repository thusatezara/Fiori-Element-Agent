# Fiori Elements Agent 작업 지침

## 언어

- 사용자와의 대화, 설명, 계획 및 프로젝트 문서는 한국어로 작성한다.
- 코드, 명령어, 파일명, 경로, API 이름, 설정 키 및 기술 식별자는 원문 영어를 유지한다.
- 번역보다 기술적 정확성이 중요한 용어는 영어를 유지하고 필요한 경우 한국어 설명을 덧붙인다.

## 문서의 역할

- 이 파일은 Codex의 작업 방식만 정의한다.
- `.specify/memory/constitution.md`는 프로젝트 전체의 비타협 원칙과 거버넌스를 정의한다.
- `specs/<feature>/spec.md`는 무엇을 만들지, `plan.md`는 어떻게 만들지,
  `tasks.md`는 어떤 순서로 실행할지를 정의한다.
- `.agents/skills/*/SKILL.md`가 Skill의 실행 기준이다. 같은 위치의 `SKILL.ko.md`는
  학습용 참고본이며 실행 지침으로 사용하지 않는다.

## Spec Kit 워크플로

- 새 기능은 원칙적으로 `specify → clarify/checklist → plan → tasks → analyze → implement → converge`
  순서로 진행한다.
- 각 단계에서는 현재 단계의 산출물만 변경하고, 다음 단계는 선행 산출물을 검토한 후 시작한다.
- 사용자가 여러 단계를 명시적으로 요청하지 않았다면 완료한 단계와 권장 다음 단계를 보고하고 멈춘다.
- 요구사항과 구현이 달라지면 관련 Spec, Plan 및 Tasks를 함께 최신 상태로 유지한다.

## 판단과 불확실성

- 저장소와 제공된 자료에서 확인한 사실, 작업을 위한 가정, 사용자 결정이 필요한 미결정 사항을 구분한다.
- 제품 동작, SAP 업무 규칙, 권한, 데이터 의미 및 외부 시스템 계약을 근거 없이 확정하지 않는다.
- 결과를 실질적으로 바꾸는 미결정 사항은 구현 전에 사용자에게 확인한다.

## 변경과 검증

- 작업 전에 관련 지침과 기존 변경 상태를 확인하고, 요청 범위에 필요한 최소 변경만 수행한다.
- 사용자가 만든 변경을 보존하며 관련 없는 파일을 정리하거나 되돌리지 않는다.
- 변경한 범위에 맞는 테스트, 정적 분석 또는 빌드 검증을 수행하고 결과를 보고한다.
- 검증할 수 없는 항목은 완료로 간주하지 않고 이유와 남은 확인 방법을 기록한다.
- 비밀정보는 저장소, 로그, 예제 또는 문서에 기록하지 않는다.
- 커밋, 배포 및 외부 시스템 변경은 사용자가 명시적으로 요청한 경우에만 수행한다.
