{
  "name": "fiori-application-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Protocol-driven SAP BTP solution planner and Fiori application generator",
  "type": "module",
  "scripts": {
    "generate": "node src/cli/generate.mjs",
    "plan": "node src/cli/plan.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
