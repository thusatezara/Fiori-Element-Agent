{
  "name": "fiori-application-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Protocol-driven SAP Fiori application generator",
  "type": "module",
  "scripts": {
    "generate": "node src/cli/generate.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
